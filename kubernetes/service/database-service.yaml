apiVersion: v1
kind: Service
metadata:
  name: database-service
  namespace: ${DATABASE_NS}
spec:
  type: ClusterIP
  selector:
    app: database
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
